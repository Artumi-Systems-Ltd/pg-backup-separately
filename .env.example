# .env example - please copy to .env for your system

PGUSER=your_pg_user         # Replace with your PostgreSQL user
PGPASSWORD=your_pg_password # Replace with your PostgreSQL password
PGHOST=localhost            # PostgreSQL host
PGPORT=5432                 # PostgreSQL port
BACKUP_DIR=/path/to/backup  # Backup directory (ensure it exists)
